<Page
    x:Class="ToxWinApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToxWinApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="FriendsListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
            <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="TabNavigation" Value="Local"/>
            <Setter Property="IsHoldingEnabled" Value="True"/>
            <Setter Property="Margin" Value="0,0,18,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ListViewItemPresenter CheckHintBrush="{ThemeResource ListViewItemCheckHintThemeBrush}" CheckBrush="{ThemeResource ListViewItemCheckThemeBrush}" ContentMargin="4" ContentTransitions="{TemplateBinding ContentTransitions}" CheckSelectingBrush="{ThemeResource ListViewItemCheckSelectingThemeBrush}" DragForeground="{ThemeResource ListViewItemDragForegroundThemeBrush}" DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}" DragBackground="{ThemeResource ListViewItemDragBackgroundThemeBrush}" DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}" FocusBorderBrush="{ThemeResource ListViewItemFocusBorderThemeBrush}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" Padding="{TemplateBinding Padding}" PointerOverBackgroundMargin="1" PlaceholderBackground="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" PointerOverBackground="{ThemeResource ListViewItemPointerOverBackgroundThemeBrush}" ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}" SelectedPointerOverBorderBrush="{ThemeResource ListViewItemSelectedPointerOverBorderThemeBrush}" SelectedForeground="{ThemeResource ListViewItemSelectedForegroundThemeBrush}" SelectedPointerOverBackground="{ThemeResource ListViewItemSelectedPointerOverBackgroundThemeBrush}" SelectedBorderThickness="{ThemeResource ListViewItemCompactSelectedBorderThemeThickness}" SelectedBackground="{ThemeResource ListViewItemSelectedBackgroundThemeBrush}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dummy data to assist in design the layout -->

        <local:Friend x:Key="Friend1" Name="Sample Friend 1" Status="Sample Status 1" />
        <local:Friend x:Key="Friend2" Name="Sample Friend 2" Status="Sample Status 2" />
        <local:Conversation x:Key="conv1">
            <local:Message Sender="{StaticResource Friend1}" Content="Hey how are you ?"></local:Message>
            <local:Message Sender="{StaticResource Friend2}" Content="fine, thanks"></local:Message>
        </local:Conversation>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBox x:Name="myIDtxt" HorizontalAlignment="Left" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Margin="30,28,0,0" Width="715" Height="59"/>

        <ListView x:Name="friendsListView"
          Margin="278,169,858,34" SelectionMode="Single" ItemContainerStyle="{StaticResource FriendsListViewItemStyle}" Background="#FF8B8B8B" SelectionChanged="SelectedFriendChanged">

            <ResourceDictionary x:Name="Friend1" />
            <ResourceDictionary x:Name="Friend2" />

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="60" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="60" Height="60">
                            <Image Source="{Binding Image}" Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="{Binding Name}" TextWrapping="NoWrap"/>
                            <TextBlock Text="{Binding Status}" TextWrapping="NoWrap"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBox x:Name="sendMessageText" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Margin="553,669,0,0" Width="685" Height="65" PlaceholderText="Write a message"/>
        <ListView x:Name="messagesListView" HorizontalAlignment="Left" Height="527" Margin="553,114,0,0" VerticalAlignment="Top" Width="784" ItemsSource="{StaticResource conv1}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="60" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="60" Height="60">
                            <Image Source="{Binding Sender.Image}" Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="{Binding Sender.Name}" TextWrapping="NoWrap" FontStyle="Italic"/>
                            <TextBlock Text="{Binding Content}" TextWrapping="NoWrap"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Content="Send" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="1260,666,0,0" Height="71" Width="80" Click="SendClick"/>
        <ListView x:Name="requestsListView"
        	Margin="30,169,1123,34" SelectionMode="Single" ItemContainerStyle="{StaticResource FriendsListViewItemStyle}" Background="#FF8B8B8B" SelectionChanged="SelectedRequestChanged">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="60" Margin="6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}" Width="60" Height="60">
                            <Image Source="{Binding Image}" Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="{Binding Name}" TextWrapping="NoWrap"/>
                            <TextBlock Text="{Binding Status}" TextWrapping="NoWrap"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ResourceDictionary x:Name="Friend3" />
            <ResourceDictionary x:Name="Friend4" />
            <TextBlock Height="100" TextWrapping="Wrap" Text="TextBlock" Width="100"/>

        </ListView>
        <TextBlock HorizontalAlignment="Left" Margin="30,114,0,0" TextWrapping="Wrap" Text="Requests" VerticalAlignment="Top" FontSize="36" Width="150"/>
        <TextBlock HorizontalAlignment="Left" Margin="278,121,0,0" TextWrapping="Wrap" Text="Friends" VerticalAlignment="Top" FontSize="36"/>
        <TextBox x:Name="requestIDText" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Margin="974,28,0,0" Width="234" Height="37" PlaceholderText="Write Tox ID Here"/>
        <Button x:Name="sendRequestButton" Content="Send Request" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="1210,25,0,0" Height="43" Width="130" Click="SendRequestClick"/>

    </Grid>
</Page>
